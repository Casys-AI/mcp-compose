/**
 * Host integration layer for mcp-compose.
 *
 * Thin type-only module defining the contracts for host applications
 * that embed composite UIs. No runtime code — just types and docs.
 *
 * @module host
 */

export type { CompositeUiHost, HostConfig } from "./types.ts";
